Flow 2 - Pending Tasks Only

1. Support for Content Elements (Flow 2 Integration)

Required Elements:
- Text
- Video
- Image
- Text over Image
- Image as Background of Text

Status:
- Text, image, and video support exist.
- Layered element models/components exist in `proposal-elements`.
- Full native support of all 5 element types inside the main Flow 2 editor still needs completion/verification.

Pending Work:
- [x] Ensure `Text over Image` and `Image as Background of Text` are available as native insert/edit options in Flow 2.
- [x] Verify these layered elements preserve layout and export correctly in the same workflow as existing widgets.